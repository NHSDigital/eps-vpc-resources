rules:
  unpinned-images:
    # these workflows use unpinned images because they are using a full image passed in that contains the tag
    ignore:
      - cdk_release_code.yml:50:7
      - cdk_package_code.yml:21:7
